# Multi-Year Tax Optimizer

Single-page tool for modeling multi-year federal + AMT + state + local tax under RSU / ISO / NSO strategy choices.

Live: https://timarnold192842.github.io/tax-planner-pages/

Build: static HTML/JS. No server, no backend. All data in `localStorage`; export/import JSON.
